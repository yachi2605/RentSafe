This folder should contain state-specific tenant law documents (PDFs or text files) used for RAG.

Add files named with the state abbreviation or full name to improve retrieval quality (e.g., `IL_tenant_law.pdf`).
